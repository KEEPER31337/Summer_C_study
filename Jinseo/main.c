#include <stdio.h>

int main()
    {
        int h,m,n;

        scanf("%d %d", &h, %m);
        if (m >= 45)
        {
            m -= 45;
        }
        else
        {
            n = 45-m;
            m = 60 - n;
            if (h ! 0)
            {
                h -= 1;
            }
            else
            {
                h = 23;
            }

        }


    }